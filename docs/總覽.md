# Vibe — Claude Code Marketplace 總覽

## 專案定位

**Vibe** 是 Claude Code marketplace（複數 plugins 容器），採用模組化架構讓多個 plugin 獨立開發、獨立安裝。

**第一個 plugin** 是 `forge`（造工具的工具）— 提供建立、驗證、更新 Claude Code plugin 組件的能力。

## 設計哲學

| # | 原則 | 說明 |
|:-:|------|------|
| 1 | **自然語言優先** | `$ARGUMENTS` 模式 — 使用者說意圖，Claude 判讀語意 + 組合執行 |
| 2 | **對外介面最小化** | 只暴露必要的 skill，腳本/模板/驗證全部內部化 |
| 3 | **語意自動化** | SKILL.md 定義能力邊界和規則，Claude 自行決定執行步驟 |
| 4 | **自動驗證閉環** | PostToolUse hook → 寫完就檢查，使用者無感 |
| 5 | **對話即文檔** | 對話中確定的方向立即歸檔 |

## 架構模型

```
Marketplace（vibe/）
├── Plugins = 模組功能（5 個，獨立安裝）
│   ├── forge/          ← 造工具的工具（✅ 完成）
│   ├── flow/           ← 開發工作流 + session 管理
│   ├── sentinel/       ← 品質全鏈（lint + test + review）
│   ├── patterns/       ← 語言/框架模式庫
│   ├── evolve/         ← 知識進化 + 文件自動化
│   └── collab/         ← 多模型協作
├── Skills = 能力載體（使用者介面）
├── Agents = AI 能力（子代理）
├── Hooks = 事件驅動自動化
├── Scripts = 內部實作（DRY）
└── Templates = 標準化產出
```

## Marketplace Plugin 全覽

| Plugin | 定位 | Skills | Agents | Hooks | 優先級 |
|--------|------|:------:|:------:|:-----:|:------:|
| `forge` | 造工具的工具 | 4 | 0 | 4 | ✅ 完成 |
| `flow` | 開發工作流 + session | 5 | 2 | 4 | 高 |
| `sentinel` | 品質全鏈守衛 | 8 | 4 | 5 | 高 |
| `patterns` | 語言/框架模式庫 | 8 | 0 | 0 | 中 |
| `evolve` | 知識進化 + 文件 | 4 | 1 | 2 | 低 |
| `collab` | 多模型協作 | 2 | 0 | 1 | 低 |

## Forge Plugin — 4 個核心 Skills

| Skill | 命名空間 | 能力 |
|-------|---------|------|
| `scaffold` | `/forge:scaffold` | 建立 plugin 骨架、驗證整體結構、更新 manifest |
| `skill` | `/forge:skill` | 建立 skill、驗證、更新、管理 references |
| `agent` | `/forge:agent` | 建立 agent、驗證、更新、引導 permissionMode 選擇 |
| `hook` | `/forge:hook` | 建立 hook + 對應 script、驗證、更新 hooks.json |

**使用方式**（自然語言）：
```
/forge:skill 建立 git-flow，支援 conventional commits
/forge:scaffold 驗證 my-plugin
/forge:hook 建立 pre-commit 檢查，跑 eslint
```

## 架構決策紀錄

| 決策 | 結論 | 原因 |
|------|------|------|
| 單一 vs 複數 plugin | Marketplace（複數） | 可擴展，各 plugin 獨立 |
| Script/Template 定位 | 內部子功能，非獨立 skill | 不暴露給使用者 |
| 驗證腳本位置 | 各 skill 的 `scripts/` | 單一職責 |
| 共用函式庫位置 | `plugin/scripts/lib/` | 被 ≥2 skill 使用才共用 |
| SKILL.md 設計 | 能力邊界+規則，非指令路由 | 自然語言優先 |
| 腳本對外可見性 | 完全內部化 | 減少使用者認知負擔 |
| Hook 語言 | Node.js（非 bash） | 跨平台相容（源自 ECC 經驗） |

## 目錄結構

```
vibe/                                       # Marketplace 根目錄
├── .claude-plugin/
│   └── marketplace.json                    # Marketplace manifest
├── CLAUDE.md                               # 專案規則
├── docs/
│   ├── 總覽.md                             # ← 本文件
│   ├── ECC研究報告.md                       # ECC 深度分析
│   ├── 開發路線圖.md                        # 開發進度
│   └── reference/                          # 6 份組件規格書
│       ├── plugin-spec.md
│       ├── skill-spec.md
│       ├── agent-spec.md
│       ├── hook-spec.md
│       ├── script-spec.md
│       └── template-spec.md
└── plugins/
    ├── forge/                              # 造工具的工具 ✅
    │   ├── .claude-plugin/plugin.json
    │   ├── skills/ (scaffold, skill, agent, hook)
    │   ├── hooks/hooks.json
    │   ├── scripts/lib/
    │   └── templates/components/
    ├── flow/                               # 開發工作流 + session 🔲
    ├── sentinel/                           # 品質全鏈守衛 🔲
    ├── patterns/                           # 語言/框架模式庫 🔲
    ├── evolve/                             # 知識進化 + 文件 🔲
    └── collab/                             # 多模型協作 🔲
```

## 參考文檔

| 文檔 | 用途 |
|------|------|
| `docs/reference/plugin-spec.md` | Plugin manifest、marketplace、目錄結構規格 |
| `docs/reference/skill-spec.md` | SKILL.md frontmatter 10 欄位、字元預算、context:fork |
| `docs/reference/agent-spec.md` | Agent 6 permissionModes、Task(agent_type)、memory |
| `docs/reference/hook-spec.md` | 14 事件、3 hook 類型、hookSpecificOutput |
| `docs/reference/script-spec.md` | 入口腳本+共用函式庫、安全規範 |
| `docs/reference/template-spec.md` | 組件模板+專案模板、佔位符規範 |

## 學習與參考來源

- **ECC**（everything-claude-code）— Anthropic x Forum Ventures 黑客松冠軍作品
  - 42K+ GitHub stars
  - 13 agents、28+ skills、30+ commands
  - 完整的 hooks + scripts + rules 體系
  - 詳見 → `docs/ECC研究報告.md`
