# Vibe — 開發路線圖

> **底層先行，由下而上**：基礎錯了就全錯，所以模板、驗證、參考文檔必須最先完成。
> **目標平台**：macOS only（不需跨平台，bash 腳本即可）。

---

## 總覽

| Phase | 名稱 | 目標 | 狀態 |
|:-----:|------|------|:----:|
| 0 | 基礎設施 | 目錄結構 + 模板 + 驗證 + 參考文檔 | ✅ |
| 0.5 | ECC 研究 | 深度分析黑客松冠軍作品 | ✅ |
| 1 | Forge SKILL.md | 4 個核心 skill 的 SKILL.md | ✅ |
| 2 | 整合測試 | plugin 載入 + skill 呼叫 + 驗證閉環 | ✅ |

---

## Phase 0 — 基礎設施 ✅

> 所有後續開發都依賴這個階段。

| # | 項目 | 產出路徑 | 狀態 |
|:-:|------|---------|:----:|
| 0-1 | Marketplace manifest | `.claude-plugin/marketplace.json` | ✅ |
| 0-2 | Forge plugin.json + 目錄骨架 | `plugins/forge/` 完整目錄 | ✅ |
| 0-3 | common.sh 共用函式庫 | `scripts/lib/common.sh` | ✅ |
| 0-4 | validate-common.sh 驗證框架 | `scripts/lib/validate-common.sh` | ✅ |
| 0-5 | 7 個 component templates | `templates/components/*.{md,json,sh}` | ✅ |
| 0-6 | 4 組驗證腳本 | 各 `skills/*/scripts/validate*.sh` | ✅ |
| 0-7 | 6 份參考文檔 | `docs/reference/*.md` | ✅ |

### 驗收標準

| # | 條件 | 通過 |
|:-:|------|:----:|
| A0-1 | Claude Code 能識別 vibe plugin | ✅ |
| A0-2 | 驗證腳本全部 PASS | ✅ |
| A0-3 | 參考文檔覆蓋所有組件類型 | ✅ |

---

## Phase 0.5 — ECC 深度研究 ✅

> 學習黑客松冠軍的實戰經驗，驗證我們的架構決策。

| # | 項目 | 產出 | 狀態 |
|:-:|------|------|:----:|
| 0.5-1 | Clone 並分析 ECC repo | `/tmp/ecc/` | ✅ |
| 0.5-2 | 13 agents 完整分析 | `docs/ECC研究報告.md` §2 | ✅ |
| 0.5-3 | 28+ skills 完整分析 | `docs/ECC研究報告.md` §3 | ✅ |
| 0.5-4 | 30+ commands 完整分析 | `docs/ECC研究報告.md` §4 | ✅ |
| 0.5-5 | Hooks + Scripts 架構分析 | `docs/ECC研究報告.md` §5-6 | ✅ |
| 0.5-6 | Rules + Contexts 分析 | `docs/ECC研究報告.md` §7 | ✅ |
| 0.5-7 | 設計差異與啟發整理 | `docs/ECC研究報告.md` §8-9 | ✅ |
| 0.5-8 | docs/ 破壞式重組（中文檔名） | `docs/總覽.md` + `docs/ECC研究報告.md` + `docs/開發路線圖.md` | ✅ |

### ECC 研究關鍵發現

| 發現 | 對 Vibe 的影響 |
|------|---------------|
| Hook scripts 全用 Node.js | 考慮遷移 bash → Node.js |
| skills 用目錄路徑自動掃描 | forge plugin.json 可採用 |
| hooks.json 自動載入 | 不需在 plugin.json 宣告 |
| Rules 無法透過 plugin 分發 | 需其他機制分享規則 |
| ECC Skill = 靜態知識庫 | 確認 Vibe Skill 是動態能力模組的差異化方向 |
| Frontmatter 極簡使用 | 大部分 skill 只需 name + description |

---

## Phase 1 — Forge SKILL.md ✅

> 4 個核心 skill 的能力定義。

| # | 項目 | 產出路徑 | 依賴 | 狀態 |
|:-:|------|---------|:----:|:----:|
| 1-1 | skill SKILL.md | `skills/skill/SKILL.md` | 0-6 | ✅ |
| 1-2 | agent SKILL.md | `skills/agent/SKILL.md` | 0-6 | ✅ |
| 1-3 | scaffold SKILL.md | `skills/scaffold/SKILL.md` | 0-6 | ✅ |
| 1-4 | hook SKILL.md | `skills/hook/SKILL.md` | 0-6 | ✅ |

### SKILL.md 設計原則（基於 ECC 研究）

- **能力邊界 + 規則**，非步驟式指令路由
- **自然語言 $ARGUMENTS**：使用者說意圖，SKILL.md 定義 Claude 可以做什麼
- **Reference 引用**：每個 skill 的 `references/` 目錄放對應 spec
- **驗證腳本整合**：SKILL.md 指示 Claude 在產出後執行驗證

---

## Phase 2 — 整合測試 ✅

> 驗證 forge plugin 可正常載入和使用。

| # | 項目 | 驗證方式 | 依賴 | 狀態 |
|:-:|------|---------|:----:|:----:|
| 2-1 | Plugin 載入測試 | `claude --plugin-dir ./plugins/forge` | 1-* | ✅ |
| 2-2 | Skill 呼叫測試 | `/forge:scaffold`、`/forge:skill`、`/forge:agent`、`/forge:hook` | 2-1 | ✅ |
| 2-3 | 驗證閉環測試 | `/forge:agent` 建立 code-reviewer → V-AG 18/18 PASS | 2-2 | ✅ |

### 整合測試發現

| 發現 | 影響 | 處置 |
|------|------|------|
| `/plugin` 覆蓋內建指令 | skill 名稱與內建指令衝突 | 改名 `plugin` → `scaffold` ✅ |
| validate.sh sed 邊界 bug | frontmatter 提取失敗 | forge:agent 自修復 ✅ |
| hook SKILL.md 範例路徑 WARN | V-SK-18 誤報（佔位符非真實引用） | 低優先級，不影響功能 |

---

## Marketplace Plugin 規劃

> ECC 把所有功能塞進一個 plugin。Vibe 依據**單一職責 + 獨立安裝**拆分為多個模組。

### Plugin 全覽（5 個，合併後）

| # | Plugin | 定位 | Skills | Agents | Hooks | Scripts | 優先級 | 狀態 |
|:-:|--------|------|:------:|:------:|:-----:|:-------:|:------:|:----:|
| 0 | `forge` | 造工具的工具 | 4 | 0 | 4 | 6 | — | ✅ |
| 1 | `flow` | 開發工作流 + session | 5 | 2 | 4 | 7 | 高 | 🔲 |
| 2 | `sentinel` | 品質全鏈守衛 | 8 | 4 | 5 | 6 | 高 | 🔲 |
| 3 | `patterns` | 語言/框架模式庫 | 8 | 0 | 0 | 0 | 中 | 🔲 |
| 4 | `evolve` | 知識進化 + 文件 | 4 | 1 | 2 | 2 | 低 | 🔲 |
| 5 | `collab` | 多模型協作 | 2 | 0 | 1 | 0 | 低 | 🔲 |

> 從原本 9 個合併為 6 個（含 forge）：sentinel + testkit → sentinel，flow + session → flow，learner + docs → evolve。

### 各 Plugin 說明

#### 1. flow — 開發工作流（含 session 管理）

**完整開發生命週期**：SessionStart → 規劃 → 架構 → compact → checkpoint → SessionEnd。

| 組件 | 內容 |
|------|------|
| Skills | plan、architect、compact、checkpoint、env-detect |
| Agents | planner（唯讀）、architect（唯讀） |
| Hooks | SessionStart（載入 context + 環境偵測）、SessionEnd（儲存 context）、PreToolUse（suggest-compact）、PreCompact（log） |
| Scripts | session-start/end.js、suggest-compact.js、log-compact.js + lib（session-manager/env-detector/counter） |

#### 2. sentinel — 品質全鏈守衛（含測試）

**靜態分析 → 格式化 → 測試 → 安全掃描 → 深度審查**。

| 組件 | 內容 |
|------|------|
| Skills | review、lint、format、security、tdd、e2e、coverage、verify |
| Agents | code-reviewer（唯讀）、security-reviewer（唯讀）、build-error-resolver（可寫）、e2e-runner（可寫） |
| Hooks | PostToolUse（auto-lint, auto-format, test-check）、PreToolUse（danger-guard）、Stop（console-log-check） |
| Scripts | auto-lint/format.js、check-console-log.js、danger-guard.js + lib（tool-detector/lang-map） |

#### 3. patterns — 語言/框架模式庫

**純知識庫**：Claude 寫碼時自動參考。

| 組件 | 內容 |
|------|------|
| Skills | coding-standards、frontend-patterns、backend-patterns、db-patterns、typescript/python/go-patterns、testing-patterns |
| 特性 | 純 SKILL.md，無 hooks/scripts/agents，ECC 靜態知識庫模式 |

#### 4. evolve — 知識進化（含文件自動化）

**從開發中學習 + 保持文件同步**。

| 組件 | 內容 |
|------|------|
| Skills | learn（知識提取）、evolve（instincts 進化）、doc-gen（文件生成）、doc-sync（文件同步） |
| Agents | doc-updater（可寫） |
| Hooks | SessionEnd（evaluate-session）、SessionStart（load-instincts） |
| Scripts | load-instincts.js + lib（instinct-store） |

#### 5. collab — 多模型協作

**Claude 主導，其他模型做 audit**。需外部 API。

| 組件 | 內容 |
|------|------|
| Skills | multi-plan（多模型規劃）、multi-review（多模型審查） |
| Hooks | PreToolUse（model-switch 提醒，可選） |

### 依賴關係

```
獨立               核心雙引擎               進階
┌──────────┐    ┌────────────┐
│ patterns │    │    flow    │ ← 規劃+session
└──────────┘    └──────┬─────┘
                       │ 可選增強
                ┌──────▼─────┐
                │  sentinel  │ ← 品質全鏈
                └──────┬─────┘
                       │ 可選增強
                ┌──────▼─────┐    ┌────────────┐
                │   evolve   │    │   collab   │
                └────────────┘    │ 需外部 API │
                                  └────────────┘
```

### 建議開發順序

1. **flow** — 規劃+session，開發流程起點
2. **sentinel** — 品質全鏈，日常最常用
3. **patterns** — 知識庫（結構簡單，可穿插）
4. **evolve** → **collab**

> 詳細設計文件見 `docs/ref/`，總覽見 `docs/ref/index.md`。
